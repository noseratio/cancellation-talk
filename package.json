{
  "scripts": {
    "start": "snowpack dev",
    "lint": "eslint",
    "build": "browserify packages/public-common.js -o public/src/public-common.js && snowpack build",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1"
  },
  "dependencies": {
    "cancellable": "file:packages\\cancelable",
    "prex": "^0.4.7"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "browserify": "^17.0.0",
    "eslint": "^7.14.0",
    "serve": "^11.3.2",
    "snowpack": "^2.17.1"
  }
}
